{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal desktop capability: core window/event APIs only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
