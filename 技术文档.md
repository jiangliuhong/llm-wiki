# 知识库实现技术开发文档

## 1. 项目定位

本项目是一个本地知识库索引与检索系统，面向仓库内的业务文档提供文件浏览、分块查看、全文检索和向量检索能力。

当前实现基于 Node.js、SQLite、sqlite-vec、SQLite FTS5 和 Next.js。系统默认扫描 `docs` 目录，将文档文件解析为文本分块，写入本地 SQLite 数据库，并通过命令行和 Web UI 提供检索能力。

当前 embedding 实现是确定性的本地 fake embedding，不调用外部 API，因此项目在依赖安装完成后可以离线运行。

## 2. 总体架构

系统由四层组成：

1. 文档源层：仓库内的 `docs` 目录。
2. 索引构建层：`scripts/kb` 下的初始化、扫描、切片、embedding 生成和入库脚本。
3. 数据访问层：`src/lib/kb.ts` 封装只读数据库访问、统计、文件列表、分块详情和混合检索。
4. 展示与接口层：Next.js API routes 和 `KbViewer` 前端页面。

核心数据流：

```text
docs 文档
  -> npm run kb:index
  -> 文件扫描
  -> 文本切片
  -> files / chunks
  -> chunks_fts / vec_chunks
  -> CLI search 或 Next.js API
  -> Web UI 展示检索结果和分块详情
```

## 3. 目录与关键文件

```text
scripts/kb/
  config.mjs       知识库配置
  init-db.mjs      SQLite schema 初始化
  index-files.mjs  文件扫描、切片、索引写入
  search.mjs       CLI 检索入口
  embedding.mjs    本地 embedding 生成

src/lib/
  kb.ts            知识库数据访问与混合检索核心逻辑

src/app/api/kb/
  stats/route.ts       索引状态 API
  files/route.ts       文件列表 API
  files/[id]/route.ts  文件分块 API
  chunks/[id]/route.ts 单个分块详情 API
  search/route.ts      混合检索 API

src/components/
  kb-viewer.tsx     知识库浏览器主界面
  kb-file-tree.ts   文件树构建逻辑
  fetch-json.ts     前端请求封装
```

## 4. 配置设计

知识库配置集中在 `scripts/kb/config.mjs`。

关键配置：

```js
export const kbConfig = {
  dbPath: ".codex-kb/index.db",
  include: ["docs"],
  exclude: ["node_modules", ".git", ".codex-kb", "dist", "build"],
  chunk: {
    maxChars: 1200,
    overlap: 200
  },
  embedding: {
    dimensions: 1536
  }
};
```

设计要点：

- `dbPath` 固定为仓库内本地索引库 `.codex-kb/index.db`。
- `include` 当前只扫描 `docs`，避免把运行结果、源码和临时文件混入业务知识库。
- `exclude` 过滤依赖、构建产物、Git 元数据和索引库自身。
- `chunk.maxChars` 控制单个检索单元大小。
- `chunk.overlap` 保留相邻分块上下文，降低切片边界造成的信息损失。
- `embedding.dimensions` 必须与 `vec_chunks` 表定义一致。

## 5. 数据库模型

数据库由 `scripts/kb/init-db.mjs` 初始化，使用 `better-sqlite3` 访问 SQLite，并加载 `sqlite-vec` 扩展。

### 5.1 files

保存文件级元数据。

字段包括：

- `id`：文件 ID。
- `path`：仓库相对路径，唯一。
- `sha256`：文件内容 hash，用于判断内容是否变化。
- `mtime`：文件修改时间。
- `size`：文件大小。
- `language`：根据扩展名识别的语言类型。
- `indexed_at`：最近索引时间。

### 5.2 chunks

保存文本分块。

字段包括：

- `id`：分块 ID。
- `file_id`：所属文件 ID。
- `chunk_index`：文件内分块序号。
- `content`：分块文本。
- `start_line` / `end_line`：分块在原文件中的起止行号。

### 5.3 chunks_fts

SQLite FTS5 虚表，用于全文检索。

```sql
CREATE VIRTUAL TABLE IF NOT EXISTS chunks_fts
USING fts5(content, content='chunks', content_rowid='id');
```

它以 `chunks.id` 作为 `rowid`，因此全文检索结果可以直接回连到原始分块。

### 5.4 vec_chunks

sqlite-vec 虚表，用于向量近邻检索。

```sql
CREATE VIRTUAL TABLE IF NOT EXISTS vec_chunks
USING vec0(
  embedding float[1536]
);
```

同样使用 `chunks.id` 作为 `rowid`，以便向量结果回连到 `chunks` 和 `files`。

## 6. 索引构建流程

索引入口是：

```bash
npm run kb:index
```

对应脚本为 `scripts/kb/index-files.mjs`。

### 6.1 文件发现

索引脚本从 `kbConfig.include` 配置的路径开始递归扫描。当前只扫描 `docs`。

支持的文件类型包括：

- `.js`
- `.mjs`
- `.cjs`
- `.ts`
- `.tsx`
- `.jsx`
- `.md`
- `.mdx`
- `.json`
- `.txt`
- `.yml`
- `.yaml`

扫描过程中会把路径标准化为 POSIX 风格，确保不同操作系统下数据库中的路径格式稳定。

### 6.2 变更判断

每个文件读取后会计算 `sha256`，并结合 `mtime`、`size` 与数据库中的旧记录比较。

如果三者均未变化，则跳过该文件，避免重复切片和重复生成 embedding。

如果文件发生变化，则更新 `files` 记录，删除该文件旧的 chunks、FTS 记录和向量记录，然后重新索引。

如果数据库中存在但本次扫描未发现的文件，则视为 stale file，会从索引中删除。

### 6.3 文本切片

切片逻辑由 `splitIntoChunks(text)` 实现。

默认策略：

- 每个分块最多约 `1200` 字符。
- 相邻分块重叠 `200` 字符。
- 如果截断点附近存在换行，会优先在换行处截断。
- 空白内容不会生成 chunk。
- 每个 chunk 记录原文的起止行号。

该设计让检索结果能返回具体路径和行号范围，便于人工回到原文核对。

### 6.4 写入索引

每个分块会写入三处：

1. `chunks`：保存分块原文和行号。
2. `chunks_fts`：保存全文检索内容。
3. `vec_chunks`：保存分块 embedding。

三类数据共享同一个 chunk ID，方便检索结果合并。

索引写入运行在 SQLite transaction 中，避免部分文件更新失败后索引状态不一致。

## 7. Embedding 实现

当前 `scripts/kb/embedding.mjs` 中的 `generateEmbedding(text)` 是本地确定性实现：

1. 遍历 1536 个维度。
2. 对维度序号和文本内容做 SHA256。
3. 将 hash 值映射到 `[-1, 1]`。
4. 对整个向量做 L2 归一化。
5. 返回 `Float32Array`。

优点：

- 不依赖网络。
- 不依赖外部 API key。
- 相同文本总是生成相同向量。
- 便于本地开发和测试 sqlite-vec 流程。

限制：

- 它不是真实语义 embedding。
- 向量相似度不能可靠表达业务语义相似度。
- 当前混合检索中，真正稳定可依赖的是 FTS 精确匹配和路径/行号定位。

后续可替换为真实 embedding，例如 OpenAI Embeddings。替换时需要保证返回向量维度与 `kbConfig.embedding.dimensions` 和 `vec_chunks` schema 一致。

## 8. 检索流程

检索入口有两个：

```bash
npm run kb:search -- "query"
```

以及：

```text
GET /api/kb/search?q=query&limit=8
```

核心逻辑位于 `src/lib/kb.ts` 的 `searchKnowledgeBase(query, limit)`。

### 8.1 向量检索

查询文本先生成 embedding，然后在 `vec_chunks` 中执行近邻查询：

```sql
WHERE v.embedding MATCH ?
  AND k = ?
ORDER BY v.distance
```

返回字段包括：

- chunk ID
- 文件路径
- 起止行号
- 分块内容
- 向量距离

### 8.2 全文检索

同一个查询文本会在 `chunks_fts` 中执行 FTS5 查询：

```sql
WHERE chunks_fts MATCH ?
ORDER BY bm25(chunks_fts)
```

返回字段包括：

- chunk ID
- 文件路径
- 起止行号
- 分块内容
- BM25 rank

如果 FTS 查询因为特殊字符失败，API 会返回 warning，并保留向量检索结果。

### 8.3 结果合并与排序

向量结果和 FTS 结果按 chunk ID 合并。

结果来源分为三类：

- `vector+fts`：同时命中向量和全文。
- `fts`：只命中全文。
- `vector`：只命中向量。

排序优先级：

1. `vector+fts`
2. `fts`
3. `vector`

同类结果内部按各自 score 排序：

- 向量结果按 distance 升序。
- FTS 结果按 BM25 rank 升序。

最终返回指定 limit 内的结果，并为每条结果生成 preview。

## 9. 数据访问层

`src/lib/kb.ts` 是服务端核心模块。

主要职责：

- 以只读模式打开 `.codex-kb/index.db`。
- 校验必要表是否存在。
- 提供索引健康状态。
- 查询文件列表。
- 查询文件下所有分块。
- 查询单个分块详情。
- 执行混合检索。

主要导出函数：

```ts
getKbStats()
listFiles(options)
getFileDetail(fileId)
getChunkDetail(chunkId)
searchKnowledgeBase(query, limit)
```

所有数据库读取通过 `withDb(callback)` 包装，确保每次访问后关闭连接。

## 10. API 设计

Next.js API routes 仅负责解析请求参数、调用 `src/lib/kb.ts`、返回 JSON。

### 10.1 统计接口

```text
GET /api/kb/stats
```

返回：

- 数据库路径。
- files/chunks/FTS/vector 数量。
- 最早和最新索引时间。
- 按语言统计。
- 按根目录统计。
- 数据库和 schema 健康状态。

### 10.2 文件列表接口

```text
GET /api/kb/files?page=1&pageSize=50&q=keyword
```

返回分页文件列表。`q` 用于按路径做 LIKE 过滤。

### 10.3 文件详情接口

```text
GET /api/kb/files/:id
```

返回文件元信息以及该文件下的 chunk 摘要列表。

### 10.4 分块详情接口

```text
GET /api/kb/chunks/:id
```

返回单个 chunk 的完整内容。

### 10.5 搜索接口

```text
GET /api/kb/search?q=query&limit=8
```

返回混合检索结果。

## 11. 前端实现

前端主组件是 `src/components/kb-viewer.tsx`。

主要功能：

- 展示索引数据库路径。
- 展示文件数、分块数、FTS/向量记录数、索引状态。
- 文件树浏览。
- 按路径过滤文件。
- 查看文件下的 chunk 列表。
- 查看单个 chunk 内容。
- 执行混合检索。
- 标识检索结果来源：向量、全文、向量 + 全文。

页面入口是 `src/app/page.tsx`，直接渲染 `KbViewer`。

前端通过 `fetchJson` 请求本地 API，所有数据仍由服务端读取本地 SQLite。

## 12. CLI 工作流

首次使用：

```bash
npm install
npm run kb:init
npm run kb:index
```

检索：

```bash
npm run kb:search -- "PL MySQL 业务表 参数集"
```

启动 Web UI：

```bash
npm run dev
```

默认开发端口在 `package.json` 中配置为 `8001`。

## 13. 当前实现特点

### 13.1 优点

- 完全本地化，适合私有业务文档。
- SQLite 单文件部署简单。
- FTS 和向量检索数据共用 chunk ID，合并结果简单。
- 分块保留行号，方便定位原文。
- 索引支持增量更新，未变化文件会跳过。
- Web UI 可查看索引状态、文件树和检索结果。

### 13.2 局限

- 当前 embedding 不是语义模型，向量检索质量有限。
- FTS 查询对特殊字符敏感，例如 `P&L` 这类字符串可能需要拆成 `P L` 或改用上下文中文词。
- 切片策略是按字符和换行切分，还未按 Markdown 标题、SQL 语句、表结构等语义边界切分。
- 当前索引只扫描文本类文件，不处理 PDF、Excel、Word 等二进制文档。
- Web UI 当前主要是浏览和查询，不承担索引触发、重建任务管理等后台操作。

## 14. 后续演进建议

### 14.1 接入真实 embedding

将 `scripts/kb/embedding.mjs` 的 `generateEmbedding(text)` 替换为真实 embedding 服务调用。

注意事项：

- 保持返回类型为 `Float32Array`。
- 保持维度与 `kbConfig.embedding.dimensions` 一致。
- 如果更换维度，需要同步修改 `vec_chunks` schema，并重建索引。

### 14.2 优化切片策略

可以按文档类型引入更细的切片规则：

- Markdown：优先按标题层级切分。
- SQL：优先按 CTE、DDL、字段段落或完整查询块切分。
- 表字段说明：保留同一张表的字段在同一批 chunk 中。
- P&L 文档：按数据分层、MySQL 表、数仓表、参数集操作、排障口径等主题切分。

目标是提高 `kb:search` 的命中准确率和结果 preview 的可读性。

### 14.3 查询预处理

可在搜索前增加 query normalize：

- 去除或转义 FTS 敏感字符。
- 对 `P&L` 同时扩展为 `PL`、`P L`。
- 对中英文术语建立同义词扩展。
- 对表名、字段名、指标名保留精确匹配优先级。

### 14.4 索引管理能力

后续可以在 Web UI 中增加：

- 当前索引是否过期。
- 最近一次索引日志。
- 手动触发重建索引。
- 文件变更检测。
- stale file 明细。

### 14.5 文档质量闭环

结合当前 AGENTS 规则，文档从 `temp` 进入 `docs` 前应先做内容边界分析和拆分确认。这样可以让索引 chunk 更聚焦，提升检索结果质量。

## 15. 维护注意事项

- 修改 `docs` 后，需要在用户确认后运行 `npm run kb:index` 更新索引。
- 修改 `scripts/kb/config.mjs` 的 chunk 或 embedding 配置后，通常需要重建索引。
- 修改 `vec_chunks` 维度后，必须重建数据库或迁移 schema。
- 如果搜索结果明显偏弱，优先检查：
  - 文档是否已进入 `docs`。
  - 是否运行过 `npm run kb:index`。
  - FTS 查询是否包含特殊字符。
  - 目标文档是否被切得过碎或过宽。
  - 当前 fake embedding 是否导致向量结果没有业务语义。
